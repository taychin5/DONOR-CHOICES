<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test_chat_app_two">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".homePage.ListViewChoseActivity"></activity>
        <activity android:name=".chatMessageMain.MainChatActivity"></activity>
        <activity
            android:name=".chatMessageMain.PopChooseActivity"
            android:theme="@style/AppTheme.PopMe" />

        <activity android:name=".first_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".chatMessageMain.PopChooseDonateActivity"
            android:theme="@style/AppTheme.PopMe"/>


    </application>

</manifest>